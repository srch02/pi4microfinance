spring.application.name=piversionbd
spring.datasource.url=jdbc:mysql://localhost:3306/pidb?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Run data.sql after Hibernate creates/updates schema (seed admin + group + member)
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
# Configuration JPA/Hibernate
spring.jpa.show-sql=true
# Configuration JPA/Hibernate

spring.jpa.hibernate.ddl-auto=update
#port du serveur
server.port=8080

spring.jpa.open-in-view=false



# Mail Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=yasminesabbaghsabbagh@gmail.com
spring.mail.password=bqds eara admg aenn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=${spring.mail.username}
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# JWT
security.jwt.secret=change-this-secret-change-this-secret-change-this
security.jwt.expiration-ms=3600000

# AES-256 key used for group chat message encryption (AesEncryptionService).
# Use a strong random string; this value is just a placeholder for local dev.
security.aes.key=change-this-aes-key-change-this-aes-key

# Hedera Hashgraph (Solidari-Health blockchain). Set hedera.enabled=false to run without blockchain.
# Use kebab-case so values bind to HederaProperties (hedera.operator.* is nested and was not binding).
hedera.enabled=true
hedera.network=testnet
hedera.operator-account-id=0.0.7989863
hedera.operator-private-key=0xe57ae01f68da83a8255edd618ceb633b1ef46cecd143fc7f76f5102e2644d88e
hedera.topic-contracts=0.0.8018910
hedera.topic-transactions=0.0.8019077
hedera.contract-id=0.0.8012466
logging.level.pi.db.piversionbd=DEBUG
logging.level.com.hedera=DEBUG

# Configuration Swagger UI (un seul document, pas de groupe "default")
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.use-root-path=true
springdoc.swagger-ui.url=/v3/api-docs
springdoc.packages-to-scan=pi.db.piversionbd.controller


# Claims: after POST /api/claims, run default scoring (total=95) so auto-approval + Telegram run without admin PUT.
# Set false when real scoring is done via /api/claim-scorings (e.g. ML pipeline).
claims.auto-score-on-submit=true

# Telegram bot (notifications)
telegram.bot.enabled=true
telegram.bot.token=8564480482:AAEJhgzI37jVE-dX1yrMEfTLXIQ5d1V_zDU

# Google reCAPTCHA: site key = public (frontend widget); secret = server-only — rotate if exposed.
# Override secret in production: RECAPTCHA_SECRET_KEY=...
recaptcha.enabled=true
recaptcha.site-key=6Lf1DZ0sAAAAAOYdJN_IYQxNSlfmbpCWPCr84Z-Z
recaptcha.secret-key=${RECAPTCHA_SECRET_KEY:6Lf1DZ0sAAAAAPkhwdduUe0Rizu0BgAk3zV8hfLK}
# Used only for reCAPTCHA v3 (ignored for v2 checkbox)
recaptcha.min-score=0.5